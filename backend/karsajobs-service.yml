apiVersion: v1
kind: Service
metadata:
  name: karsajobs-be
  labels:
    app: karsajobs-be
    tier: backend
spec:
  type: NodePort
  selector:
    app: karsajobs-be
    tier: backend
  ports:
  - port: 8080
    nodePort: 30037